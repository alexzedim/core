# Secrets mount point.
#
# In production, the host directory /mnt/cert-sync/secrets/ is bind-mounted
# here (read-only) and contains authorized_key.json — the YC service account
# key with the certificate-manager.certificates.downloader role.
#
# This .gitkeep is the only thing committed under secrets/. Never commit
# the actual authorized_key.json.
